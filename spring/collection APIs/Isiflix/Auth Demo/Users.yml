info:
  name: Users
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/users
  body:
    type: json
    data: |
      {
        "login": "antonio",
        "password": "123456",
        "name": "Antonio Silva",
        "email": "antonio@email.com",
        "role": "USER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
