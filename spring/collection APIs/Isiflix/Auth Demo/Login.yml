info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/users
  body:
    type: json
    data: |
      {
        "login": "antonio",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
