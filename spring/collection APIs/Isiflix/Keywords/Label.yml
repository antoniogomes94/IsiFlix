info:
  name: Label
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/labels
  body:
    type: json
    data: |-
      {
        "value":"Velho Barreiro",
        "keywords":[
          {
           "id": 4
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
